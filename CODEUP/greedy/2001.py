'''
2001 : 최소 대금

문제 설명
파파 파스타 가게는 점심 추천 파스타와 생과일 쥬스 세트 메뉴가 인기가 좋다.
이 세트 메뉴를 주문하면 그 날의 3 종류의 파스타와 2 종류의 생과일 쥬스에서 하나씩 선택한다.
파스타와 생과일 쥬스의 가격 합계에서 10%를 더한 금액이 대금된다.
어느 날의 파스타와 생과일 쥬스의 가격이 주어 졌을 때, 그 날 세트 메뉴의 대금의 최소값을 구하는 프로그램을 작성하라.

입력
입력은 5 행으로 이루어지며, 한 줄에 하나씩 양의 정수가 적혀있다.
1행의 정수는 첫 번째 파스타 가격이다.
2행의 정수는 두 번째 파스타 가격이다.
3행의 정수는 세 번째 파스타 가격이다.
4행의 정수는 첫 번째 생과일 쥬스 가격이다.
5행의 정수는 두 번째 생과일 쥬스의 가격이다.
(모든 파스타와 생과일 쥬스의 가격은 100 원이상 2000원 이하이다.)

출력
그날 세트 메뉴의 최소 대금을 소수 첫째자리까지 출력하시오.


'''

p1 = int(input())
p2 = int(input())
p3 = int(input())
d1 = int(input())
d2 = int(input())

p = [p1, p2, p3]
d = [d1, d2]

print( format((min(p) + min(d)) * 1.1, '.1f') )

'''
Gwmini 피드백

작성하신 코드는 문제의 핵심인 **"각 카테고리에서 최솟값을 찾아 합산 후 1.1을 곱한다"**는 로직을 아주 정확하고 깔끔하게 구현하고 있습니다[cite: 2].

코드의 효율성과 가독성 측면에서 몇 가지 유용한 피드백을 드릴게요.

### 1. 입력부 최적화
현재는 변수 5개를 하나씩 선언하셨는데, 파이썬의 리스트 컴프리헨션이나 반복문을 사용하면 입력 코드를 더 간결하게 줄일 수 있습니다.

```python
# 기존 방식
p = [int(input()) for _ in range(3)] # 파스타 3개 입력
d = [int(input()) for _ in range(2)] # 주스 2개 입력
```
이렇게 작성하면 나중에 입력받아야 할 메뉴가 늘어나더라도 숫자만 바꾸면 되어 유지보수가 편해집니다.

### 2. 출력 포맷팅 (Format vs f-string)
사용하신 `format()` 함수도 훌륭하지만, 최근 파이썬(3.6+)에서는 **f-string**을 사용하는 것이 가독성이 더 좋아 권장되는 추세입니다.

*   **작성하신 방식:** `print(format((min(p) + min(d)) * 1.1, '.1f'))`[cite: 2]
*   **f-string 방식:** `print(f"{(min(p) + min(d)) * 1.1:.1f}")`

### 3. 계산 과정의 명확성
문제에서 **"가격 합계에서 10%를 더한 금액"**이라고 명시했으므로, `* 1.1`을 곱하는 로직은 수학적으로 완벽합니다[cite: 2]. 소수점 첫째 자리까지 출력하는 조건도 `'.1f'`를 통해 잘 지켜졌습니다[cite: 2].

---

### 종합 제안 코드
기존 로직을 유지하면서 조금 더 '파이썬스럽게' 다듬은 버전입니다.

```python
# 메뉴별로 입력을 리스트로 바로 받기
pastas = [int(input()) for _ in range(3)]
drinks = [int(input()) for _ in range(2)]

# 최솟값 합산 및 세금(10%) 계산
min_total = (min(pastas) + min(drinks)) * 1.1

# 소수점 첫째 자리까지 출력
print(f"{min_total:.1f}")
```

**💡 팁:** 
입력값이 100원~2000원 사이로 크지 않기 때문에, 지금처럼 `min()` 함수를 사용하는 방식이 시간 복잡도 면에서 가장 효율적입니다 ($O(N)$). 

앞서 풀으셨던 리모컨 문제처럼 그리디한 접근(가장 싼 것을 고르는 선택)이 이 문제에서도 핵심이었는데, 아주 잘 파악하셨네요! 다음 문제들도 이어서 풀어보시겠어요?

'''

'''
Gemini 추가
"리스트로 받는 게 가장 효율적인가?"라는 질문은 **메모리 효율성**과 **코드의 간결성** 두 가지 측면에서 살펴볼 수 있습니다. 결론부터 말씀드리면, 지금처럼 데이터의 개수가 아주 적은 경우(파스타 3개, 음료 2개) 리스트를 사용하는 것이 가장 깔끔하고 파이썬스러운 정석적인 방법입니다[cite: 2].

하지만 데이터가 수만 개로 늘어나거나, 메모리를 극도로 아껴야 하는 상황이라면 다른 접근법이 더 효율적일 수 있습니다. 상황별로 비교해 드릴게요.

---

### 1. 메모리 효율성: 스트리밍 방식 (Generator)
입력받은 데이터를 리스트에 모두 저장하지 않고, 들어오는 즉시 최솟값만 갱신하며 버리는 방식입니다. 데이터가 1억 개쯤 된다면 이 방식이 훨씬 효율적입니다.

```python
# 리스트에 저장하지 않고 실시간으로 최솟값 찾기
import sys

# 파스타 최솟값 찾기
min_p = float('inf')
for _ in range(3):
    price = int(sys.stdin.readline())
    if price < min_p:
        min_p = price

# 음료 최솟값 찾기
min_d = float('inf')
for _ in range(2):
    price = int(sys.stdin.readline())
    if price < min_d:
        min_d = price

print(f"{(min_p + min_d) * 1.1:.1f}")
```
*   **장점**: 메모리 사용량이 입력 데이터 양과 상관없이 일정합니다 ($O(1)$ space).
*   **단점**: 코드가 길어지고 가독성이 떨어집니다.

### 2. 실행 속도 효율성: `sys.stdin.readline`
`input()` 함수는 입력받을 때마다 프롬프트를 확인하고 문자열을 처리하는 과정이 있어 다소 느립니다. 대량의 데이터를 처리할 때는 `sys.stdin.readline`이 훨씬 빠릅니다.

```python
import sys
input = sys.stdin.readline

# 이후 코드는 동일
p = [int(input()) for _ in range(3)]
d = [int(input()) for _ in range(2)]
```

### 3. 구조적 효율성: 맵(Map) 활용
만약 입력 형식이 한 줄에 하나씩이 아니라 한 줄에 여러 개라면 `map()`을 쓰는 게 가장 빠릅니다. (현재 문제는 한 줄에 하나씩이므로 해당사항은 없지만 참고용입니다.)

---

### 요약 및 결론

| 방식 | 효율성 포인트 | 추천 상황 |
| :--- | :--- | :--- |
| **현재의 리스트 방식**[cite: 2] | **코드 가독성 & 개발 속도** | 일반적인 코딩 테스트, 적은 데이터 |
| **sys.stdin.readline** | **I/O 속도** | 입력 데이터가 10만 개 이상일 때 |
| **실시간 갱신 (Streaming)** | **메모리(RAM) 절약** | 메모리 제한이 극도로 타이트할 때 |

**"이 문제(2001번)에서는 리스트로 받는 것이 가장 효율적입니다."**[cite: 2]
데이터가 5개뿐이라 메모리 차이가 거의 없고, `min(list)` 함수는 내부적으로 C로 구현되어 있어 매우 빠르게 동작하기 때문입니다[cite: 2].

지금처럼 문제를 풀 때는 **"얼마나 읽기 쉽고 실수를 줄일 수 있는가"**에 초점을 맞춘 현재의 리스트 방식이 가장 좋은 습관입니다.

혹시 나중에 데이터가 엄청나게 많은 문제를 풀게 되면, 그때는 제가 알려드린 `sys.stdin.readline`을 떠올려 보세요!
'''
'''
# 3120 : 리모컨

## 문제 설명
컴퓨터실에서 수업 중인 정보 선생님은 냉난방기의 온도를 조절하려고 한다.
냉난방기가 멀리 있어서 리모컨으로 조작하려고 하는데, 리모컨의 온도 조절 버튼은 다음과 같다.

1) 온도를 1도 올리는 버튼
2) 온도를 1도 내리는 버튼
3) 온도를 5도 올리는 버튼
4) 온도를 5도 내리는 버튼
5) 온도를 10도 올리는 버튼
6) 온도를 10도 내리는 버튼

이와 같이 총 6개의 버튼으로 목표 온도를 조절해야 한다.
현재 설정 온도와 변경하고자하는 목표 온도가 주어지면 이 버튼들을 이용하여 목표 온도로 변경하고자 한다.
이 때 버튼 누름의 최소 횟수를 구하시오.

예를 들어, 7도에서 34도로 변경하는 경우,
7 -> 17 -> 27 -> 32 -> 33 -> 34
이렇게 총 5번 누르면 된다.

## 입력
현재 온도a 와 목표 온도b가 입력된다. ( 0 <= a , b <= 40 )

## 출력
최소한의 버튼 사용으로 목표온도가 되는 버튼의 횟수를 출력한다.

'''

'''
계획
1. 입력 = 현재 온도와 목표 온도
2. 출력 = 목표 온도가 되기까지 최소 버튼 횟수
3. 구현
    1) 10 -> 5 -> 1 순으로 나눈다. - 나머지를 다음 수로 나눔
	2) 몫은 출력값에 더한다.

관찰) 
1. 4와 9는 별도의 처리가 필요함. 1+1+1+1보다 5-1이 빠르고 5+1+1+1+1보다 10-1이 빠름. 8 또한 5+1+1+1보다 10-1-1이 빠름.
    10으로 나눈 나머지를 관찰했을 때, 1위치, 5위치, 9위치에서부터 시작해서 1 -> 증가 -> 3 <- 증가 <- 5 -> 증가 -> 7, 8 <- 증가 <- 9 방식임.
	이걸 규칙으로 만드는 것보다 그냥 10개짜리 리스트를 만들고 넣는 방식이 빠르고 편할 것이라고 판단.
2. 음수는 신경쓰지 않아도 됨. 이유는 리모컨의 온도 조절 방식이 대칭임.
    즉, x도에서 x + 25도가 되는 것은 곧 x - 25도가 되는 것과 다르지 않음. 단지 모든 동작이 반대이기만 하면 되기 때문임.
'''
now, goal = map(int, input().split())

t = now - goal
count = 0

unit = [0, 1, 2, 3, 2, 1, 2, 3, 3, 2]

if t < 0:
	t = -t
	
if t == 0:
	print(count)
	
else:
	if t % 10 == 0:
		count = t // 10 
	else:
		count += t // 10
		t = t % 10
		count += unit[t]
		print(count)


'''
Gemini 피드백
코드업 3120번 **리모컨** 문제를 그리디 알고리즘으로 잘 접근하셨습니다! 특히 목표 온도와의 차이($t$)를 구한 뒤, 10으로 나눈 나머지값들에 대해 미리 계산된 `unit` 리스트(Lookup Table)를 활용해 예외 상황(4나 9 등)을 처리하신 부분은 매우 효율적인 방식입니다.[cite: 1]

작성하신 코드보다 조금 더 간결하거나, 다른 관점에서 최적화할 수 있는 몇 가지 팁을 제안해 드립니다.

---

### 1. 코드 간결화 (Pythonic Way)
작성하신 `if-else` 구조를 조금 더 다듬으면 가독성을 높일 수 있습니다. 특히 $t=0$인 경우도 일반적인 수식에 포함될 수 있어 조건문을 줄일 수 있습니다.

```python
now, goal = map(int, input().split())
diff = abs(now - goal)  # 절댓값 함수로 음수 처리

# 10으로 나눈 몫을 먼저 더함
count = diff // 10
remainder = diff % 10

# 0~9까지의 최적 횟수를 담은 리스트 (작성하신 unit과 동일)
# 4는 '5-1'로 2번, 9는 '10-1'로 2번 등을 반영
unit = [0, 1, 2, 3, 2, 1, 2, 3, 3, 2]

count += unit[remainder]
print(count)
```

### 2. 순수 그리디(Greedy) 방식의 접근
Lookup Table(`unit`)을 쓰지 않고, 매 순간 가장 최선의 선택을 하는 그리디의 정석적인 로직으로도 구현이 가능합니다. 이 방식은 버튼의 종류가 바뀌어도 유연하게 대처할 수 있다는 장점이 있습니다.

*   **핵심 로직:** 차이값이 8 이상이면 10을 누르고, 3 이상이면 5를 누르는 식으로 범위를 좁혀가는 방식입니다.

```python
now, goal = map(int, input().split())
t = abs(now - goal)
count = 0

while t > 0:
    if t >= 8:      # 8, 9도는 10도 버튼을 누르고 반대로 조절하는 게 빠름
        t = abs(t - 10)
    elif t >= 3:    # 3, 4도는 5도 버튼을 누르는 게 빠름
        t = abs(t - 5)
    else:           # 1, 2도는 1도 버튼
        t = abs(t - 1)
    count += 1

print(count)
```

### 3. 비교 분석

| 방식 | 장점 | 특징 |
| :--- | :--- | :--- |
| **기존 방식 (Lookup Table)** | **가장 빠름** | $O(1)$의 시간 복잡도로 나머지만 확인하면 끝납니다.[cite: 1] |
| **반복문 방식 (Greedy)** | **확장성** | 버튼의 온도 단위가 복잡해져도 로직 수정이 직관적입니다. |

**결론:** 질문자님이 작성하신 **나머지를 인덱스로 활용하는 방식**은 이 문제에서 가장 효율적인 해법 중 하나입니다.[cite: 1] 다만, `abs()` 함수를 사용해 코드를 줄이고 `t == 0`일 때의 예외 처리를 일반화하면 더 깔끔한 코드가 될 것입니다.

이미 알고리즘 구현 실력이 상당하시네요! 특히 4와 9를 `5-1`, `10-1`로 해석하신 관점이 훌륭합니다.[cite: 1]
'''

	